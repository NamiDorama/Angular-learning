;
for ( = "" > Server; name < /label>
    < !-- < input-- >
    !--type;  = "text"-- >
    !--(function () {
        function class_1() {
        }
        return class_1;
    }()))
     = "form-control"-- >
        !--(input);
"onUpdateServerName($event)" > -- >
    type;
"text";
var default_1 = (function () {
    function default_1() {
    }
    return default_1;
}());
"form-control"[(ngModel)] = "serverName" >
    !-- < p > {};
{
    serverName;
}
/p>-->
    < button;
var default_2 = (function () {
    function default_2() {
    }
    return default_2;
}());
"btn btn-primary"[disabled] = "!allowNewServer"(click) = "onCreateServer()" > Add;
Server
    < /button>
    < !-- < p[innerText];
"allowNewServer" > /p>-->
    < !-- < p > {};
{
    serverCreationStatus;
}
/p>-->
    < p * ngIf;
"serverCreated" > Server;
was;
created, server;
is;
{
    {
        serverName;
    }
}
/p>ng serve
    < app - server > /app-server>;
